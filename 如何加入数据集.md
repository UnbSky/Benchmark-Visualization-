# 如何加入新数据集

只改 `data/benchmarks.js`。保存后刷新页面即可，不必改 HTML / CSS / JS。

分数一律写成 **0–100 的百分数**（例如 `80.9` 表示 80.9%）。图谱、条形图、宏观统计都会读这个文件。

---

## 1. 准备来源

在 `sources` 数组里加一条，`id` 不要与已有项重复：

```js
{
  id: "my-paper",
  title: "论文或官方页面标题",
  org: "作者 / 机构",
  date: "2026-01",
  url: "https://...",
}
```

表格「来源」列会显示：可点击的 `org`（链到 `url`）+ 该条成绩的 `locus`（表 / 图 / 节）。

---

## 2. 模型（如尚未收录）

在 `models` 里补：

```js
{ id: "my-model", name: "显示名", org: "机构名" }
```

`org` 会用在模型图谱的颜色和筛选芯片上（如 Anthropic / OpenAI / Google）。

---

## 3. 领域（可选）

现有四类（对齐 LLM Agent 评测综述的粗粒度划分，不必再拆细）：

- `swe` 软件工程 — 仓库补丁、终端任务
- `gui` 计算机与网页 — 桌面 / 移动 GUI、站点操作
- `tools` 工具与对话 — 多轮政策、API / MCP
- `work` 知识工作 — 职场交付、科研复现、ML 工程

若现有 `taxonomy.domains` 确实不够用，再加一项：

```js
{ id: "browser", label: "浏览器 Agent", desc: "真实网页上的长程操作" }
```

`evalTargets`、`environments` 同样按需追加，并在数据集里引用它们的 `id`。

---

## 4. 添加 benchmark

向 `benchmarks` 追加一个对象。必填字段：

| 字段 | 含义 |
|---|---|
| `id` | 稳定英文 id，如 `swe-bench-verified` |
| `name` / `short` | 全称与图谱短名 |
| `family` / `year` | 系列名、发布年 |
| `domainIds` | 所属领域 id 数组，决定图谱连线与筛选高亮 |
| `evalTarget` / `environment` | 对应 taxonomy 中的 id |
| `domainNeed` | 数据集需要什么领域 / 数据形态 |
| `evaluates` | 评估什么能力 |
| `whyImportant` | 为何重要 |
| `metric` | 官方主指标短名，如 `% Resolved` |
| `metricExplain` | 指标定义 + 出处，会显示在悬停框和图表说明里 |
| `nInstances` | 题量 |
| `map` | 可选。不填则按领域自动分列排布 |
| `sourceIds` | 该数据集本身的文献（介绍用） |
| `results` | 成绩列表 |

数据集按 `domainIds` 的第一项自动分列，画布可滚轮缩放、拖动平移。节点多时不必手写坐标。

`map` 若仍填写会被忽略（布局由领域列计算）。

---

## 5. 成绩 `results`

每一条：

```js
{
  modelId: "claude-opus-4.5",   // 必须已在 models 中
  harness: "脚手架 / 配置，一句话",
  score: 80.9,                  // 百分数
  date: "2025-11",              // 收集时间，图表单独一列
  sourceId: "opus45-card",      // 必须已在 sources 中 → 来源列的链接
  locus: "Table 2.4.A",         // 表 / 图 / 节，如 Table 3、图 2、§8.2
  subset: "telecom",            // 可选
  notes: "可选内部备注，来源列不展示长文",
}
```

来源列只显示 **链接 + 位置**。`locus` 写清行/列/表/图即可，不要把整段说明写进去。没有 `locus` 时，页面会尝试从 `notes` 里抽出 Table / 图 / §。

同一模型、同一数据集、不同 harness 或子集，就写多条。图谱上的「当前最好」取该集 `score` 最高的一条。

---

## 6. 指标怎么写

页面**不会**自己换算指标。你写什么，图上就展示什么。

当前三套约定：

| 数据集 | `metric` | 含义 |
|---|---|---|
| SWE-bench Verified | `% Resolved` | 补丁同时通过 FAIL_TO_PASS 与 PASS_TO_PASS 的题比例 |
| Terminal-Bench 2.0 | `Resolution Rate` | 终端任务最终状态被测试判为解决的比例 |
| τ²-bench | `Pass^1` | Pass^k 在 k=1 时的成功率 |

新数据集请：

1. 用官方名称填 `metric`；
2. 用一两句话写清计算方式和文献，填进 `metricExplain`；
3. `score` 与官方报道一致（已经是百分比就直接填）。

不同指标不要当成同一把尺子。首页统计是数据集数、模型数、以及在多少个数据集上拿第一；不把不同集的分数放在一起比。

---

## 7. 最小示例

```js
// sources 中增加
{ id: "osworld-paper", title: "OSWorld", org: "Xie et al.", date: "2024", url: "https://..." }

// models 中如需
{ id: "gpt-4o", name: "GPT-4o", org: "OpenAI" }

// benchmarks 中增加
{
  id: "osworld",
  name: "OSWorld",
  short: "OSWorld",
  family: "OSWorld",
  year: 2024,
  domainIds: ["terminal"],
  evalTarget: "cli-agent",
  environment: "terminal-container",
  domainNeed: "真实桌面 OS 任务与可执行校验。",
  evaluates: "图形界面 + 终端上的计算机使用成功率。",
  whyImportant: "把 Agent 评测从仓库补丁扩展到整机操作。",
  metric: "Success Rate",
  metricExplain: "官方主指标：任务成功比例。定义见 Xie et al. OSWorld。",
  nInstances: 369,
  sourceIds: ["osworld-paper"],
  results: [
    {
      modelId: "gpt-4o",
      harness: "论文默认 computer-use agent",
      score: 12.2,
      date: "2024-04",
      sourceId: "osworld-paper",
      locus: "Table 2",
    },
  ],
}
```

保存 `data/benchmarks.js`，刷新浏览器。新节点应出现在数据集图谱；悬停可看到领域 / 评估 / 为何重要 / 指标；点击进入图表。

---

## 8. 检查清单

- [ ] `id` 全局唯一（model / source / benchmark）
- [ ] `results[].modelId`、`sourceId` 都能对上
- [ ] `domainIds` 里的 id 在 `taxonomy.domains` 中存在
- [ ] `score` 为 0–100 的数，`date` 为收集时间
- [ ] 每条成绩有 `sourceId`（链接）和 `locus`（表 / 图 / 节，简洁即可）
- [ ] `metric` + `metricExplain` 写了官方定义（一两句话即可）
- [ ] 图表只展示单个数据集，不要把不同指标的分数画在同一张图里比较
